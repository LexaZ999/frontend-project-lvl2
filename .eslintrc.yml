env:
  node: true
  es2021: true

extends:
  - airbnb-base

parserOptions:
  ecmaVersion: 12
  sourceType: module

globals:
    test: readonly
    expect: readonly
    
rules:
  no-console: 0
  import/extensions: 0
  no-underscore-dangle: [2, { "allow": ["__filename", "__dirname"] }]
